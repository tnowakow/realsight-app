{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "echo 'Starting deployment...' && (npx prisma migrate deploy && echo 'Migration successful!') || (echo 'Migration failed, retrying in 2s...' && sleep 2 && npx prisma migrate deploy && echo 'Migration successful on retry 1!') || (echo 'Migration failed, retrying in 5s...' && sleep 5 && npx prisma migrate deploy && echo 'Migration successful on retry 2!') && echo 'Starting server...' && node index.js",
    "start:robust": "sleep 5 && npx prisma migrate deploy && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.12.0",
    "prisma": "^7.8.0"
  }
}
